{
  "name": "home-todo-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute home-todo --file=../worker/schema.sql"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250430.0",
    "@types/web-push": "^3.6.4",
    "typescript": "^5.8.3",
    "wrangler": "^4.14.4"
  }
}
